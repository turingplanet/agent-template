# THIN POINTER. Copied into every member repo at creation, then detached.
# It carries NO logic — it only references the central flow by version (REFERENCE path).
# Points at the platform-owned `policies` repo (turingplanet) by version.
name: review
on:
  pull_request:
jobs:
  review:
    uses: turingplanet/policies/.github/workflows/review-reusable.yml@v1
    with:
      contract: v1
